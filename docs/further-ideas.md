# Now

Make sure all criterias are set on neutral values such as Don't care, etc by default so that all/most distros are visible by default when a new user accesses the website.

Make sure the local storage is not persisten when user clears cache / maybe even when they close the website


# Later On

## active-filters
- make the badges more visible for each user-set filter